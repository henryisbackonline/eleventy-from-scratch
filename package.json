{
    "scripts": {
		"build": "pnpm exec eleventy",
		"start": "pnpm exec eleventy --serve --quiet",
		"serve": "pnpm exec eleventy --serve --quiet"
	},
	"type": "module",
	"dependencies": {
		"@11ty/eleventy": "^3.1.2"
	}
}